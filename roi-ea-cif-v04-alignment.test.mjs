import assert from 'node:assert/strict';import test from 'node:test';
import {CIF_FRAMEWORK_VERSION,CIF_EPISTEMIC_TYPES,CIF_RELATIONSHIP_DEFINITIONS,normalizeCanonicalRelationship,validateSemanticEntailment} from './roi-ea-canonical-model.mjs';
import {evaluateCIFValidation} from './roi-ea-cif-validation-model.mjs';
test('current CIF version and existing epistemic subtypes',()=>{assert.equal(CIF_FRAMEWORK_VERSION,'0.4.1');for(const x of ['CLAIM','EVIDENCE','FACT','INFERENCE','RECOMMENDATION','FORECAST'])assert.equal(CIF_EPISTEMIC_TYPES.includes(x),true);});
test('governed relationship requires authoritative representation, scope and basis',()=>{const r=normalizeCanonicalRelationship({id:'R1',relationshipType:'ACCOUNTABLE_FOR',sourceId:'A',sourceFamily:'OF-01',sourceSubtype:'HUMAN_PERSON',targetId:'D',targetFamily:'OF-16',scope:'decision D',basisRef:'B1'});assert.equal(r.errors.length,0);assert.equal(r.representationMode,CIF_RELATIONSHIP_DEFINITIONS.ACCOUNTABLE_FOR.authoritativeRepresentation);});
test('forecast does not entail fact',()=>assert.equal(validateSemanticEntailment({sourceConcept:'FORECAST',proposedConcept:'FACT'}).valid,false));
test('R C V remain independent',()=>{const r=evaluateCIFValidation({id:'VAL1',subjectId:'CASE1',representability:'PASS',conformance:'CONDITIONAL',externalValidity:'OUTSIDE_CIF_DETERMINATION',scope:'pilot',basisRefs:['B1'],evidenceRefs:['E1'],asOf:'2026-09-25',conditions:[{id:'C1',requirement:'Monitor pilot boundary',enforcementRef:'control-1',monitorRef:'monitor-1',effectiveFrom:'2026-09-01',effectiveTo:'2026-10-01'}]});assert.equal(r.status,'PASS');assert.equal(r.overallCIFPass,null);});
