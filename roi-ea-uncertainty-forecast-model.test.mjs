import assert from 'node:assert/strict';import test from 'node:test';import {validateForecastWithUncertainty} from './roi-ea-uncertainty-forecast-model.mjs';
test('forecast stays distinct from fact and decision',()=>{const r=validateForecastWithUncertainty({forecast:{id:'F1',claimId:'C1',forecasterId:'M1',forecastType:'CONDITIONAL_FORECAST',forecastHorizon:'12 months',methodRef:'ROI-MODEL',uncertaintyProfileId:'U1'},uncertainty:{id:'U1',subjectClaimId:'C1',uncertaintySources:['EPISTEMIC_UNCERTAINTY'],uncertaintyStatus:'QUALITATIVELY_CHARACTERIZED'}});assert.equal(r.status,'PASS');assert.equal(r.forecastIsFact,false);assert.equal(r.forecastCreatesDecision,false);});
